%appinstall pandasplot
$MAMBA_EXE create --yes --channel conda-forge --name pandasplot python=3.11 &&
$MAMBA_EXE run --name pandasplot pip install pandas==2.1.1 matplotlib==3.8.0

%appenv pandasplot
export ENV_NAME=pandasplot

%apprun pandasplot
$MAMBA_EXE run --name $ENV_NAME python -c "import pandas as pd; print('pandas version: ', pd.__version__)"